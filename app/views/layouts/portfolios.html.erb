<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <%= csrf_meta_tags %>

    <meta name="keywords" content="<%= @seo_keywords %>" />

    <%= stylesheet_link_tag    'portfolios', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!--fix portfolio nav-->
    <%= render "shared/portfolio_nav" %>
    
    <%= render "shared/portfolio_head" unless logged_in?(:site_admin) %>
    
    <%= link_to "Create A New Portfolio", new_portfolio_url, class: "btn btn secondary btn-block" if logged_in?(:site_admin) %>

    <%= yield %>
    
    <%= render "shared/portfolio_footer" %>

    <%= source_helper("application") %>
  </body>
</html>
